export interface Product {
    id: string;
    productName: string;
    img: Array<{}>;
    categoryId: string;
    description: string;
    quantity: number;
    price: number;
    discount: number;
}
